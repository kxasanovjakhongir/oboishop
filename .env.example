# docker-compose variables — copy to .env (repo root) before running
# `docker compose up` or `./docker/init-letsencrypt.sh`.
DOMAIN=oboishop.uz
ADMIN_DOMAIN=admin.oboishop.uz
API_DOMAIN=api.oboishop.uz
LETSENCRYPT_EMAIL=you@example.com
