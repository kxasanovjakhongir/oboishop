# Copy to .env.development (local) or .env.production (server build).
# Vite only exposes VITE_-prefixed vars, and inlines them into the built JS —
# these are public values, not secrets.

VITE_API_URL=http://localhost:8003/api
VITE_SITE_URL=http://localhost:3000
